export enum Events {
  connection = "connection",
  connect = "connect",
  confirmed = "confiremd",
  reciveConfirmed = "reciveConfirmed",
}

export type User = {
  id: string;
  username: string;
};
